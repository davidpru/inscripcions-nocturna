name: Deploy to Dinahosting

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install npm dependencies
        run: npm ci

      - name: Build assets
        run: npm run build

      - name: Sync build files to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_KEY }}
          source: 'public/build/'
          target: '~/www/'
          strip_components: 0

      - name: Deploy application
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            cd ~/www
            git config pull.rebase false
            git pull origin main
            ~/.bin/php -d disable_functions= composer.phar install --no-dev --optimize-autoloader
            ~/.bin/php artisan migrate --force
            ~/.bin/php artisan config:cache
            ~/.bin/php artisan route:cache
            ~/.bin/php artisan view:cache
            ~/.bin/php artisan storage:link
